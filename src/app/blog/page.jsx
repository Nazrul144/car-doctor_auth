import Blog from '@/components/Blog';
import React from 'react';

export const metadata = {
    title: "Blog"
}

const BlogPage = () => {
    return (
        <div>
           <Blog/>
        </div>
    );
}

export default BlogPage;
