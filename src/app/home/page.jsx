import HomePage from '@/components/HomePage/HomePage';
import React from 'react';

export const metadata = {
    title : "Home"
}

const Page = () => {
    return (
        <div>
            <HomePage/>
        </div>
    );
}

export default Page;
